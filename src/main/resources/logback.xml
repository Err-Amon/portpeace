<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender: errors only -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <encoder>
            <pattern>%msg%n</pattern>
        </encoder>
    </appender>

    <!-- File appender: full debug history -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${user.home}/.portpeace/logs/portpeace.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${user.home}/.portpeace/logs/portpeace.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- Suppress noisy libraries at WARN level -->
    <logger name="com.zaxxer.hikari" level="WARN" />
    <logger name="com.zaxxer" level="WARN" />
    <logger name="com.mysql" level="WARN" />
    <logger name="com.mysql.cj" level="WARN" />

    <!-- Application packages default to ERROR so CLI output is clean -->
    <logger name="com.portpeace" level="ERROR" />

    <root level="DEBUG">
        <appender-ref ref="FILE" />
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>
